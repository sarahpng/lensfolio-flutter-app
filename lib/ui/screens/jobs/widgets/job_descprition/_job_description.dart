import 'package:flutter/material.dart';

class _JobDescription extends StatelessWidget {
  const _JobDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
