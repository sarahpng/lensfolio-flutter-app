part of 'button.dart';

enum AppButtonStyle {
  primary,
  primaryBorder,
  black,
  white,
  blackBorder,
  secondary,
  error,
  success,
}

enum AppButtonSize { small, medium, large }

enum AppButtonRadius { normal, rounded, capsule }

enum AppButtonState { def, pressed, disabled }
